# Assets pendientes de reemplazo

Coloca aquí los archivos reales cuando estén disponibles:

- `escudo-pbh.svg` — escudo oficial de la Peña (reemplaza `components/ShieldPlaceholder.tsx`).
- `logo-30-aniversario.svg` — logotipo conmemorativo del 30 aniversario (reemplaza `components/AnniversaryBadge.tsx`).
- `og-cover.jpg` (1200x630) — imagen para compartir en redes sociales, referenciada en `app/layout.tsx`.
- `hero-bg.jpg` — foto de fondo del Hero (afición/estadio/sede).
- `galeria/` — fotos reales para la sección de Galería (reemplaza los mosaicos de `components/Galeria.tsx`).

También falta un `favicon.ico` en `public/` (referenciado desde `app/layout.tsx`).
